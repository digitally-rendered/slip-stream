"""MCP (Model Context Protocol) server for slip-stream schema access.

Exposes slip-stream's schema registry, entity operations, and DAG
as MCP tools that AI assistants can use to understand and interact
with a running slip-stream application.

Requires ``mcp`` (optional dependency)::

    pip install slip-stream[mcp]
"""
